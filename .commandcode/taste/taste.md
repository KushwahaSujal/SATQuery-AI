- Prefers using HF transformers directly (AutoModel.from_pretrained with trust_remote_code, device_map="auto") to serve model results via frontend, rather than custom adapter/wrapper infrastructure. Confidence: 0.9
- Prefers 4-bit NF4 quantization (BitsAndBytesConfig with load_in_4bit=True, bnb_4bit_quant_type="nf4", bnb_4bit_use_double_quant=True, bnb_4bit_compute_dtype=float16) to reduce VRAM usage when loading models via HF transformers. Confidence: 0.8
- Prefers frontend UI elements to be wired up to real backend APIs rather than left as non-functional placeholders. Wants functional integration even when backend infrastructure already exists. Confidence: 0.75
- Wants manual UI controls (e.g. task type selector) preserved rather than removed in favor of backend auto-detection alone — values explicit user override options even when the system can infer automatically. Confidence: 0.65
- Expects all UI components to be fully visible, properly styled, and not cut off or truncated. Will immediately flag any visual/overflow issues and expect them fixed thoroughly. Confidence: 0.7
- Asks "how it working?" after features are implemented — wants to understand the architecture and data flow of what was built, not just have it work. Confidence: 0.6
- When a frontend-backend mismatch is found, wants a comprehensive audit of ALL integration points for similar issues — not just fixing the one reported bug. Prefers systematic, full-stack sweeps over piecemeal fixes. Confidence: 0.7
- Creates and shares structured audit/specification documents (e.g. FRONTEND_POLISH_2026-09-14.md with numbered issues C1-C12, M1-M36, L1-L30) and expects the agent to work through them methodically and address every item. Confidence: 0.8
- Strongly dislikes fabricated, hardcoded, or mock data in production code — expects all UI to display real backend data with proper null/empty handling. Will repeatedly flag fake defaults, invented values, and placeholder data. Confidence: 0.85
- Expects production-quality, error-free code output — phrases like "without errors as like professional developer" indicate a high bar for code cleanliness and correctness. Confidence: 0.75
- Prefers unavailable/disabled UI elements to be hidden entirely rather than shown greyed-out. Clean, minimal UI where only actionable controls are visible — no dead buttons cluttering the interface. Confidence: 0.8
- Prefers aggressive frontend data caching — data should only refetch when there's new data to show (e.g. after running a task) or when the cache is stale, never on page navigation or tab switching. Expects cached data to display immediately without empty-flash-then-load patterns. Confidence: 0.8
